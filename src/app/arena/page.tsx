import { ComingSoon } from "@/components/ComingSoon";

export default function ArenaPage() {
  return (
    <ComingSoon
      title="1V1 Arena"
      description="Ranked matchmaking lobby. Coming in the next pass: ELO-based opponent search, versus screen, best-of-3 with rotating criteria."
    />
  );
}
